# Conformance testing

Tests Faith against a matrix of compatibility tests × server stacks.

## Input

- `expected-matrix.json` defines the matrix of tests vs server stacks
- the rest of this folder defines the actual tests and how to run the stacks

## Output

- `matrix.json`, machine-readable result set
- `matrix.md`, a nice markdown table

<!-- we'll put a copy of the table here when we have one -->
