import wrapper from "./wrapper.js";

export const {
	Agent,
	CacheMode,
	CacheStore,
	Credentials,
	Duplex,
	ERROR_CODES,
	FAITH_VERSION,
	fetch,
	Http3Congestion,
	Redirect,
	REQWEST_VERSION,
	Response,
	USER_AGENT,
} = wrapper;

export default wrapper;
